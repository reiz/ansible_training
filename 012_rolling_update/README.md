# Configuring a load balancer with Ansible

After:

```
vagrant up
```

run:

```
ansible-playbook setup.yml
```

to setup the example application for the rolling update:

--- LIVE DEMO ---

## Done

If you are done, destroy the VMs with:

```
vagrant destroy -f
```
